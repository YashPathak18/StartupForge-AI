# StartupForge AI Infrastructure

Infrastructure-specific notes and operational placeholders for local development.

The root `docker-compose.yml` remains the single source of truth for service
orchestration. This directory does not duplicate Compose configuration.
