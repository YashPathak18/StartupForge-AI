# Redis

Redis is used for short-lived state and caching concerns where required by the
application workflow.

The root `docker-compose.yml` remains the source of truth for the local Redis
container.
